services:
  database:
    image: postgres:16
    environment:
      POSTGRES_USER: backend_user
      POSTGRES_PASSWORD: backend_user
      POSTGRES_DB: database_dev
    ports:
      - "5432:5432"

    networks:
      - boilerplate-network