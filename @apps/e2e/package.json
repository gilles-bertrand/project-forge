{
  "name": "@apps/e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end tests for the application",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "pnpm run setup && playwright test --headed",
    "test:debug": "pnpm run setup && playwright test --debug",
    "setup": "tsx scripts/setup.ts && pnpm run setup:db",
    "setup:db": "tsx scripts/setup-db.ts"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "^25.2.0"
  }
}
