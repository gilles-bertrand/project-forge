{
    "extends": ["node_modules/@libs/repo-utils/configs/oxlintrc.json"],
    "overrides": [
        {
            "files": ["src/mounters.ts"],
            "rules": {
                "max-lines": "off"
            }
        },
        {
            "files": ["tests/**/*.ts"],
            "rules": {
                "max-lines-per-function": "off"
            }
        }
    ]
}
