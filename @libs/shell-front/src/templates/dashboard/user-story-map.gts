import type { TOC } from '@ember/component/template-only';
import PlaceholderPage from '../../components/placeholder-page.gts';

const UserStoryMapTemplate: TOC<Record<string, never>> = <template>
  <PlaceholderPage @title="User Story Map" @description="Disponible en P5" />
</template>;

export default UserStoryMapTemplate;
