import type { TOC } from '@ember/component/template-only';
import PlaceholderPage from '../../components/placeholder-page.gts';

const SprintsTemplate: TOC<Record<string, never>> = <template>
  <PlaceholderPage @title="Sprints" @description="Disponible en P8" />
</template>;

export default SprintsTemplate;
