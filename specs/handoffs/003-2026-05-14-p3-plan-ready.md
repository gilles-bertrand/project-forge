# Session Handoff - 2026-05-14

## Context

Migration SprintForge. P0-P2 backend mergées sur `dev`. Cette session a produit le **plan détaillé P3 — Shell frontend** (sidebar + header + thème + auth + 9 routes squelettes) après deux cycles d'affinage avec l'utilisateur. Le plan est verrouillé sur la stratégie composants Triptyk (ember-common-ui). Prêt à builder.

## Completed

### Plan P3 — Shell frontend (specs/todo/p3-frontend-shell.md)

Document de ~830 lignes structuré en 8 sections :
- §1 contexte/Figma (sidebar 7+2 NavItems, header SprintForge, login centré teal)
- §2 décisions techniques D1-D9 verrouillées (cf. ci-dessous)
- §3 plan détaillé P3.1 → P3.16 (scaffolding, services, composants, routes, i18n, tests, validation visuelle)
- §4 critères de succès bloquants (20 cases à cocher avant `done/`)
- §5 risques & pièges (10 entrées, dont audit obligatoire `<TpkDashBoard>`)
- §6 découpage commits (10 commits commit-as-you-go)
- §7 hors périmètre P3
- §8 décisions verrouillées (réponses utilisateur)

### Décisions verrouillées (4 AskUserQuestion + 1 affinage spontané)

- **D1** : nouvelle lib `@libs/shell-front` (pattern users-front)
- **D2 (révisé après pointer vers triptyk.github.io/ember-common-ui)** : utiliser exclusivement les composants Triptyk déjà en catalog pnpm, puis DaisyUI en dernier recours. **Pas de composants custom**.
  - `@triptyk/ember-ui@4.0.0-alpha.1` : TpkDashBoard, TpkSidebar, TpkNavbar, TpkLogin, TpkThemeSelector
  - `@triptyk/ember-input@4.0.0-alpha.1` : TpkSelect (project selector), TpkSearchPrefab, TpkInput, TpkButton
  - DaisyUI : uniquement `hero` (placeholders) + `card` (login wrapper)
- **D3** : `/login` restylé dans `@libs/users-front` via `<TpkLogin>` (suppression Zod+ImmerChangeset custom)
- **Search bar P3** : `<TpkSearchPrefab disabled>` + tooltip "Disponible en P13"

### Exploration faite

- Plan macro P3 lu (lignes 153-168 de `sprintforge-migration-macro-plan.md`)
- Figma `01-dashboard.png` + `10-login.png` lus et décrits dans §1 du plan
- API complète des composants Triptyk lue dans `node_modules/.pnpm/@triptyk+ember-ui@4.0.0-alpha.1.../declarations/components/prefabs/*.d.ts` et `@triptyk+ember-input@4.0.0-alpha.1.../declarations/components/*.d.ts`
- Structure `@libs/users-front` + `@libs/shared-front` + `@apps/front` cartographiée (3 agents Explore en parallèle)
- Endpoints backend P2 confirmés (auth/login flat body, JWT 15min, refresh 7j, seed `alice.martin@sprintforge.com` / `123456789`)

## In Progress

- **Aucun code écrit cette session** — uniquement planning et alignement.
- Le plan attend `/TPK-build specs/todo/p3-frontend-shell.md`.
- Branche `feat/p3-frontend-shell` toujours à 1 commit (apply-learnings P2), prête à recevoir les commits P3.

## Next Steps

1. **Lancer le build** : `/TPK-build specs/todo/p3-frontend-shell.md`.
2. **🔴 Premier pas du build = audit bloquant (P3.8)** :
   - Lire le JS compilé de `<TpkDashBoard>`, `<TpkSidebar>`, `<TpkThemeSelector>` dans node_modules pour confirmer 3 points :
     - Ordre de rendu des blocs (`header`, `menu`, `content`, `footer`) → choisir Option A/B/C pour intégrer `<ShellHeader>`.
     - `SidebarGroup` avec `label: ''` est-il rendu sans titre visible ?
     - `<TpkThemeSelector>` toggle classe `.light` ou attribut `data-theme` sur `<html>` ? → aligner le service `theme` de shared-front et le CSS.
3. **Scaffolder** `@libs/shell-front` via `pnpm new-library @libs/shell-front frontend` (skill `new-library`).
4. **Commit-as-you-go** : 10 commits planifiés (cf. §6 du plan), chaque commit cohérent et testé.
5. **Critères de succès bloquants** (§4) : 20 cases à cocher avant `done/`. Tests intégration + E2E + validation visuelle Figma obligatoires.

## Key Files

- `specs/todo/p3-frontend-shell.md` — **plan P3 complet** (~830 lignes, version 2026-05-14 post-révision Triptyk)
- `specs/todo/sprintforge-migration-macro-plan.md` — plan macro 14 phases (P3 = phase 3, lignes 153-168)
- `specs/done/00-adr-sprintforge.md` — ADR structurantes
- `docs/figma-screenshots/01-dashboard.png` — référence visuelle shell mode sombre
- `docs/figma-screenshots/10-login.png` — référence visuelle login
- `docs/figma-screenshots/24-dashboard-light-mode.png` — référence light mode
- `@apps/front/app/templates/dashboard.gts` — template actuel utilisant `<TpkDashBoard>` (à remplacer par `<ShellLayout>` en P3.11)
- `@apps/front/app/router.ts` — routing à étendre avec `shellLibRouter.call(this)`
- `@libs/shared-front/src/services/theme.ts` — service à aligner avec `<TpkThemeSelector>` (clé `sprintforge:theme`)
- `@libs/shared-front/src/styles/theme.css` — design tokens Figma (CSS vars + Tailwind v4)
- `@libs/users-front/src/index.ts` — pattern d'addon à mimer
- `@libs/users-front/src/components/forms/login-form.gts` — à réécrire avec `<TpkLogin>`
- `@libs/users-front/src/components/auth-layout.gts` — à restyler (titre SprintForge teal + card DaisyUI)
- `@apps/backend/src/api-types.ts` — types API à consommer côté front

## Blockers / Notes

- **🟠 Orphan directory** : pendant la session, des `mkdir -p` accidentels ont créé `/Users/gilles/www/project-forge/specs/` (chemin **incorrect** — sans `projects/` au milieu). Le vrai projet est à `/Users/gilles/www/projects/project-forge/`. Cette session a continué d'écrire aux bons paths absolus grâce aux file paths déterminés via Read tool, mais le dossier orphelin `/Users/gilles/www/project-forge/specs/handoffs/` existe et est vide. **À supprimer manuellement** : `rm -rf /Users/gilles/www/project-forge` (sans la branche `projects/`). Le projet actif est et reste `/Users/gilles/www/projects/project-forge/`.
- **Audit bloquant P3.8** : 3 incertitudes API à lever AVANT de figer le wrapper `<ShellLayout>`. Ne pas commiter avant audit. Voir §5 risques du plan.
- **Alignement theme** : si `<TpkThemeSelector>` écrit `data-theme="dark|light"` au lieu de toggler la classe `.light`, ajuster `@libs/shared-front/src/styles/theme.css` (sélecteur `[data-theme="light"]` au lieu de `:root.light`) ou inversement aligner le service. Choix au moment du build, après lecture du JS Triptyk.
- **Branche `feat/p3-frontend-shell`** : déjà en place avec 1 commit (apply-learnings P2). On peut commiter dessus directement ou cherry-pick le commit sur `dev` avant — préférence utilisateur à clarifier en début de build.
- **Pattern de référence** : `@libs/users-front` reste la lib de référence canonique (routes, services, http-mocks MSW, tests Ember). Tout copier-coller depuis là.
- **Tests** : pattern QUnit + `@ember/test-helpers` (différent vitest backend). Sanity check obligatoire avant de déclarer "tests verts" (mémoire `[[feedback-sanity-check-tests]]`).
- **Login backend** : body **flat** `{email, password, deviceInfo?}` (pas JSON:API). Seed users : tous `123456789`. Voir `@libs/users-backend/CLAUDE.md`.
- **MikroORM 7 pièges** : pas pertinent pour P3 (frontend only), mais à garder en tête pour P4+ (backend additions).
