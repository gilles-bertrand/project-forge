---
title: "Handoff #010 — P6+P7+P8 livrés, PR #12 verte, mémoires mises à jour"
created: 2026-05-16
branch: feat/p8-sprints-front
pr: 12 (https://github.com/gilles-bertrand/project-forge/pull/12)
depends_on: PR #11 (chore/tpk-apply-learnings-p6-p7) + PR #9/#10 déjà mergées
status: ready-for-review
---

# Session Handoff — 2026-05-16

## Context

Session ultra-longue couvrant P6 (Tasks), P7 (Kanban), rétrospective, TPK-apply-learnings, et P8 (Sprints). 3 PRs ouvertes + 2 déjà mergées dans `dev`. Parallélisation sous-agents systématique (6 agents P6, 5 agents P7, 6 agents P8). Toutes CI vertes après fix lint.

---

## Completed

### P6 — Tasks domain (PR #9, mergée dans dev)
- `TasksService` étendu (5 méthodes + 4 types)
- MSW mocks : 18 tasks + sub-resources (comments, history, assignees)
- 2 badges : `TaskStatusBadge`, `TaskPriorityBadge`
- `TaskCard` (kanban + dashboard variants)
- `AddTaskModal` (10 champs, multi-assignées, POST + refresh)
- `TaskDetailModal` (header 4 badges + 3 tabs + footer)
- Callback propagation `TaskRow @onOpen → UserStoryRow → EpicRow`
- i18n namespace `tasks/` + `backlog.modal.addTask/taskDetail`
- Fix P5 bug i18n : `userStoryMap.*` → `user-story-map.*` (folder namespace)
- Fix WarpDrive schemas globaux manquants (`EpicSchema`, `UserStorySchema`, `TaskSchema` dans store.ts)
- Fix sub-resources cache : `loadComments/loadHistory/loadAssignees` via fetch natif
- 39/39 tests, lint vert, 7 screenshots visuels

### P7 — Kanban (PR #10, mergée dans dev)
- `KanbanBoard`, `KanbanColumn`, `SprintHeader`, `KanbanFilters`
- `TaskCard @draggable` + dragstart HTML5
- `TasksService.update` overload `{ refresh?: boolean }`
- Route `/kanban`, template, drag-drop optimistic + rollback
- MSW sprints stub (2 sprints) dans backlog-front (migré en P8)
- Placeholder shell-front `/kanban` retiré
- i18n `backlog.kanban.*`
- 44 tests (39 P5/P6 + 5 P7... après P8 : 5 backlog tests P7 + 5 sprints P8)
- CI fail PR #10 corrigé (TS2353 + no-unnecessary-type-assertion)

### TPK-apply-learnings (PR #11, ouverte)
- 6 améliorations aux commands `.claude/commands/` :
  - `TPK-build.md` : lint depuis racine + validation visuelle bloquante
  - `TPK-plan.md` : checklist audit Ember frontend + parallélisation
  - `TPK-pickup.md` : surfacer top-5 mémoires feedback en début de session
- `CLAUDE.md` : distinction `dev:front` (MSW off) vs `cd @apps/front && pnpm start` (MSW on)

### Rétrospective P6+P7
- `docs/retrospectives/retrospective-2026-05-16-001-p6-p7-build.md`
- 9 erreurs, 9 leçons, 8 améliorations

### P8 — Sprints (PR #12, verte, prête à merger)
- **Nouvelle lib `@libs/sprints-front`** (Embroider v2, rollup, vitest)
- `SprintSchema` WarpDrive réel + enregistrement dans store.ts
- `SprintsService` (8 méthodes, fetch natif — bypass JSONAPICache)
- 6 mock sprints (1 active, 4 planned, 1 completed) + handlers MSW complets
- `SprintCard` (variantes active/planned/completed, dropzone drag-drop)
- `AddSprintModal` (5 champs + validation endDate > startDate)
- `SprintsPagination` (< > avec range "Sprint X à Y sur Z")
- `TaskRow @draggable` étendu (symétrique TaskCard P7)
- Route `/sprints` + template (intra-page drag-drop, side-panel backlog)
- Placeholder shell-front `/sprints` retiré
- i18n namespace `sprints/{fr-fr,en-us}.yaml`
- 5 nouveaux tests sprints-front
- **Cycle deps cassé** : backlog-front ↔ sprints-front (type local dans backlog-front)
- **CI fails résolus** :
  - pnpm-lock.yaml désynchronisé (tu as commité `469b426`)
  - `.prettierignore` absent dans scaffold (`f2960a2`)
  - Lint turbo global avant push — règle mémorisée

### Mémoires ajoutées (session complète)
- `feedback-i18n-folder-namespace` — folder name = namespace ember-intl (kebab)
- `feedback-warpd-schemas-global` — JSONAPICache filtre si schema absent → fetch natif
- `feedback-lint-before-push` — **toujours `pnpm turbo lint` AVANT push**

---

## In Progress

### Cycle type dupliqué (tech debt)
- `Sprint` est défini dans `@libs/sprints-front/src/schemas/sprints.ts` (schema WarpDrive) ET dans `@libs/backlog-front/src/schemas/sprints.ts` (type-only local sans WarpDrive). Nécessaire pour éviter le cycle deps bidirectionnel. **Sync manuelle requis** si champs ajoutés.
- Solution P11/P12 : déplacer dans `@libs/shared-front/src/types/sprint.ts`.

### Drag-drop limité à intra-page
- `/sprints` : side-panel "Backlog" dans la même page → drag vers SprintCard. Cross-route HTML5 impossible.
- Solution P12 : `@dnd-kit` ou Pointer Events.

---

## Next Steps

1. **Merger PR #12** (`feat/p8-sprints-front` → `dev`) — CI verte, prête.
2. **Merger PR #11** (`chore/tpk-apply-learnings-p6-p7` → `dev`) — améliorations TPK.
3. **P9 — Time Tracking** (prochaine phase macro-plan) :
   - `@libs/time-tracking-front` (nouvelle lib, pattern sprints-front)
   - Route `/time-tracking`
   - Composer `LogTimeModal` (réutilisé depuis le bouton disabled P6/P7/P8)
   - `TimeEntriesService` (log, list, summary)
   - Backend `@libs/time-tracking-backend` existe déjà (P2)
4. **Valider visuellement P8** : `/sprints` avec proj-1 → 6 sprints, pagination, card active avec progress, drag task.

---

## Key Files

### Nouveaux dans P8 (lib sprints-front)
- `@libs/sprints-front/src/schemas/sprints.ts` — SprintSchema WarpDrive + types
- `@libs/sprints-front/src/services/sprints.ts` — SprintsService 8 méthodes
- `@libs/sprints-front/src/http-mocks/sprints.ts` — 6 sprints + 6 handlers MSW
- `@libs/sprints-front/src/components/sprint-card.gts` — card variantes active/planned/completed
- `@libs/sprints-front/src/components/add-sprint-modal.gts` — modal 5 champs
- `@libs/sprints-front/src/components/sprints-pagination.gts` — pagination
- `@libs/sprints-front/src/routes/dashboard/sprints.ts` — model
- `@libs/sprints-front/src/templates/dashboard/sprints.gts` — page orchestration
- `@libs/sprints-front/.prettierignore` — ignoré dist/declarations (**manquant dans scaffold !**)

### Modifiés dans P8
- `@apps/front/app/services/store.ts` — ajout SprintSchema
- `@apps/front/app/routes/application.ts` — wiring allSprintsHandlers + initializeSprintsLib
- `@apps/front/package.json` — dep @libs/sprints-front ajoutée
- `@libs/backlog-front/src/schemas/sprints.ts` — type local (anti-cycle)
- `@libs/backlog-front/src/components/task-row.gts` — @draggable + dragstart
- `@libs/backlog-front/src/http-mocks/backlog.ts` — retrait handlers sprints migrés

### Specs
- `specs/done/p6-tasks-front.md`
- `specs/done/p7-kanban-front.md`
- `specs/done/p8-sprints-front.md`
- `specs/todo/` — vide (P9 à planifier)
- `docs/retrospectives/retrospective-2026-05-16-001-p6-p7-build.md`

---

## Blockers / Notes

### Règle critique mémorisée : `feedback-lint-before-push`
**TOUJOURS `pnpm turbo lint` depuis la racine AVANT chaque `git push`.**
PR #9, #10, #12 ont toutes fail CI sur des warnings non détectés par lint partiel.

### Règle critique mémorisée : `feedback-warpd-schemas-global`
JSONAPICache filtre silencieusement si le `type` JSON:API n'a pas de schema dans `store.ts`. Bypass : `fetch()` natif + flatten attributes. Pour les entities principales (Sprint, Epic, etc.) → enregistrer dans store.ts.

### Scaffold nouvelle lib — checklist
Quand on scaffold une nouvelle lib Embroider v2, vérifier que **tous** ces fichiers sont copiés depuis `@libs/projects-front` :
- `.prettierignore` ← **SOUVENT OUBLIÉ par les sous-agents**
- `rollup.config.mjs`, `addon-main.cjs`, `babel.config.cjs`, `babel.publish.config.cjs`
- `eslint.config.mjs`, `vite.config.mts`, `tsconfig.json`, `tsconfig.publish.json`
- `src/index.ts` (initialize stub, PAS moduleRegistry)
- `tests/app.ts`, `tests/test-helper.ts`
- `pretest: rollup -c` dans `package.json`

### Plan macro restant
- `specs/done/` : P0, P1, P2, P3, P3.5, P4, P5, P6, P7, P8
- Prochaine phase : **P9 — Time Tracking**
- Backend `@libs/time-tracking-backend` déjà livré (P2)
- Composant "Logger du temps" disabled sur toutes les modales actuelles → P9 l'active

### PRs en attente
| PR | Branch | Statut |
|---|---|---|
| #11 | `chore/tpk-apply-learnings-p6-p7` | Ouverte, CI verte |
| #12 | `feat/p8-sprints-front` | Ouverte, CI verte |
