# Session Handoff - 2026-05-13

## Context

Migration SprintForge. Phases P0, P1 et P2 backend terminées et mergées sur `dev`. Rétro de session P2 réalisée, learnings appliqués aux commandes. Prêt à démarrer P3 — Shell frontend.

## Completed

### P2 — APIs JSON:API (PR #4, mergée)
- **`@libs/scrum-backend`** : CRUD JSON:API pour Project, Epic, UserStory, Task, Sprint + sous-routes (members, comments, attachments, history, assignees) + relations cross-agrégats (8 routes) + actions Sprint start/stop + `/search` + `/dashboard`
- **`@libs/time-tracking-backend`** : CRUD TimeEntry avec `meta.totalHours` agrégé
- **`@apps/backend`** : ScrumModule + TimeTrackingModule montés, `api-types.ts` régénéré (40 paths)
- **118 tests intégration verts** (102 scrum + 16 time-tracking, testcontainer Postgres 16)
- Smoke test validé : dashboard → 17h Claire (KPI Figma)

### Refactor TimeTrackingPort (PR #5, mergée)
- Interface `TimeTrackingPort` dans `@libs/scrum-backend/src/dashboard/time-tracking.port.ts`
- `SqlTimeTrackingAdapter` dans `@apps/backend/src/adapters/sql-time-tracking.adapter.ts`
- scrum-backend redevenu bounded context pur (plus de cross-lib dep sur time-tracking)
- 103 tests scrum verts (+1 test injection port à 17h)

### Rétro + Apply Learnings (commit sur feat/p3-frontend-shell)
- Rétro : `docs/retrospectives/retrospective-2026-05-13-001-p2-backend-apis.md`
- Learnings appliqués :
  - `/TPK-build` : étape 5b "verify success criteria" bloquante + note commit-as-you-go
  - `/TPK-plan` : tests intégration = critères bloquants non substituables par smoke test
  - `/new-library` : boilerplate backend avec `#tests/*` + squelette tests
  - `@libs/backend-shared/CLAUDE.md` : section pièges MikroORM 7 (raw(), SqlEntityManager, insert() bypass)
  - `@libs/users-backend/CLAUDE.md` : avertissement body flat `POST /auth/login`

## In Progress

- **Branche `feat/p3-frontend-shell`** : existe déjà avec 1 seul commit (le apply-learnings). Pas encore de code P3. La branche part de `dev` après merge PR #5.

## Next Steps

1. **Pousser le commit apply-learnings qui est sur `feat/p3-frontend-shell`** (ou cherry-pick sur `dev` d'abord si on préfère séparer les concerns)
2. **Lancer le plan P3** : `/TPK-plan P3 — Shell frontend (Layout, thème, auth)`
   - P3 consommera `@apps/backend/src/api-types.ts` pour le typage fetch côté front
   - Libs front existantes : `@libs/shared-front` (design tokens, service theme), `@libs/users-front` (auth UI)
   - App front : `@apps/front` (Ember 6, Embroider/Vite)
3. **Builder P3** : `/TPK-build specs/todo/p3-*.md`

## Key Files

- `specs/done/p2-backend-apis.md` — plan P2 complété (référence)
- `specs/todo/sprintforge-migration-macro-plan.md` — plan macro 14 phases (P3 = phase 3)
- `specs/done/00-adr-sprintforge.md` — décisions DDD structurantes
- `@apps/backend/src/api-types.ts` — types API générés (à consommer en P3 côté front)
- `@apps/backend/src/adapters/sql-time-tracking.adapter.ts` — adapter hexagonal (pattern à répliquer si besoin)
- `@libs/shared-front/src/styles/theme.css` — design tokens SprintForge (dark/light, Tailwind v4)
- `@libs/shared-front/src/services/theme.ts` — service toggle thème
- `@libs/users-front/` — addon Ember auth UI (à inspecter en début de P3)
- `@libs/scrum-backend/src/dashboard/time-tracking.port.ts` — interface port (pattern hexagonal)

## Blockers / Notes

- **Branche `feat/p3-frontend-shell` contient le commit apply-learnings** : si on veut garder les learnings séparés de P3 côté historique git, cherry-pick le commit sur `dev` puis reset la branche. Si on s'en fout, commencer P3 directement sur cette branche.
- **Pattern de référence P3** : `@libs/users-backend` + `@libs/users-front` sont les références canoniques à mimer pour les routes et addons Ember. Observer la structure avant de créer de nouvelles libs front.
- **Smoke test backend validé** : le seeder `development.seeder.ts` peuple 7 users, 3 projets, 13 tasks, 10 time-entries (17h Claire exactes). Relancer `pnpm seed` si la DB est vide.
- **Tests unitaires front** : Ember 6 utilise `@ember/test-helpers` + QUnit. Pattern différent des tests vitest backend.
- **`pnpm dev`** : démarre back + front en parallèle. Backend sur port 8000 (configurable dans `.env`).
- **MikroORM 7 pièges** : voir `@libs/backend-shared/CLAUDE.md` section "Pièges MikroORM 7" (ajoutée dans cette session).
- **Login body flat** : `POST /auth/login` accepte `{email, password}` direct, pas JSON:API — voir `@libs/users-backend/CLAUDE.md`.
